{
  "name": "expenses",
  "version": "3.1.0",
  "private": true,
  "license": "MIT",
  "author": "Crispin Kirchner",
  "dependencies": {
    "bootstrap": "^5.1.3",
    "bootstrap-icons": "^1.7.2",
    "chart.js": "^3.7.0",
    "chartjs-chart-treemap": "^2.0.2",
    "pouchdb-browser": "^7.2.2",
    "pouchdb-find": "^7.3.0",
    "react-scripts": "^5.0.1",
    "sass": "^1.51.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "deploy.bat",
    "int": "npm run build && npx http-server build -S -C cert.pem -K key.pem -p 3000"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
